# QC-REVIEW-2026-07-21 — full luxury-bar audit, every page

Method: three parallel forensic passes (all HTML/sections, all CSS, all JS + data),
cross-referencing every contact fact across files, plus headless Chrome renders at
1440 and 390/430 and a post-JS DOM dump to confirm nothing broke. Bar: "built by
Apple/IBM, priced like Gucci." 100+ issues found; the resolvable ones are fixed.

## Facts verified consistent (no action)
Email `sabaglobalventures.bd@gmail.com`, WhatsApp `+8801830187381`, phone
`+8801879175319`, director "Shah Alam / Managing Director", address "Green House
Mansion, Jhautola, Adarsha Sadar, Cumilla 3500", brand "Saba Global Ventures"
(plural) — all agree across index JSON-LD, config.js and every section fallback.

## Fixed — HTML / sections
1. footer: `{SALES_EMAIL}` / `{PHONE}` / `{ADDRESS_CUMILLA}` fallbacks → real values (no broken placeholders if JS fails).
2. footer: dead `href="#"` Privacy/Terms links removed (broken links read cheap) — real legal pages still TODO (see Deferred).
3. contact: same three `{CURLY}` fallbacks → real values; stale header comment corrected.
4. contact: `#f-reply` `autocomplete="tel"` → `off` (field takes number OR email).
5. about: "See the Line Card →" (pointed at #products, not the line card) → "See the Catalogue →".
6. markets: fixed ungrammatical `aria-label` ("Saba Global Ventures's").
7. cta: "Line Card (PDF)" (it's an HTML page, not a PDF) → "View the Line Card".
8. index: removed stale `{WEBSITE}` comment; added `og:site_name`, `og:locale`, `og:locale:alternate` (ar).
9. theme-color unified to `#081B45` across index + all three redirect stubs (was `#071C4D`).
10. redirect stubs (about/products/contact): added `robots: noindex, follow`.
11. defs: removed stray `*/` inside an HTML comment.
12. furniture: preloader emblem alt standardized to "Saba Global Ventures crest"; removed retired `.scroll-progress` element.

## Fixed — CSS
13. tokens: off-palette ocean glow `#34A3E0` → `--ocean-400`; added `--error`/`--error-soft`/`--error-line` semantic tokens.
14. markets: dead pin `:hover` (identical fill) given a real change + added keyboard `:focus-visible` ring; `#fff` → `--rice-white`.
15. contact: hardcoded reds → error tokens; `<option>` colours → `--ink-900`/`--powder-100`; off-palette ocean → `--ocean-400`.
16. components: `.badge--ocean` + `.toast--ok/--err` off-palette colours tokenized/corrected; removed dead top-bar `.scroll-progress` rule.
17. products: faint search placeholder (.45→.6 alpha); `.pc-desc` body weight 300→400 (thin/low-contrast running text).
18. Tiny-font floor raised on uppercase micro-caps that fell to ~9px: featured `.cat` (.56/.6→.62/.66), header/admin wordmark `small` (.58→.62/.64).
19. featured / header / footer: corrected stale "dark-green" comments (the bar/footer are navy now); removed duplicate `.featured{position}` rule.
20. layout: removed empty `.hide-mobile {}` ruleset.
21. admin: removed duplicate `.admin-login .emblem` + redundant table-hover selector; `#ff9b9b` → `--error-soft`; ocean status tags tokenized.
22. rtl: added missing mirrors — modal/menu close buttons, `.capability`/`.pc-arrow` hover nudges, admin-dot inline gap.

## Fixed — JS
23. products.js: modal Tab-trap + backdrop + Escape listeners were re-added on every open (leak + duplicate firing) → attached once on the reused element.
24. nav.js: `scrollHeight`/`clientHeight` read on every scroll tick (layout thrash) → cached, recomputed on resize/load only.
25. firebase.js: fire-and-forget Firestore writes → each `.set()`/`.delete()` now has `.catch()` (no unhandled rejection on denied/offline write).
26. main.js: admin/config values now HTML-escaped before `innerHTML` (market chips, stats, certs, trade facts, FAQ, markets modal).
27. main.js: `renderSeasonNote()` null-guarded (`querySelector("span")` could throw and halt boot).
28. main.js: markets "See all" modal given focus management (move focus in, trap Tab, restore on close, remove keydown on close).
29. cta-helper.js: per-field unset warnings (was a single global one-shot); disabled `#` CTA links now `preventDefault` (no jump-to-top).
30. contact.js: form error toast now translates for the Arabic edition and focuses the first missing field.
31. i18n.js: fixed two dictionary keys broken by the copy changes above; added the missing modal-chrome strings (Varieties/Grades, Origins, Packaging, MOQ:, HS Code:), the markets-modal + empty-state strings, and a `t()` string translator.

## Deferred — needs the owner's decision (not guessed)
- footer "Sabiha Agro & Dairy **Firm**" — likely "Dairy **Farm**"? (registered name — confirm before change).
- footer "**GulfRabiit**" — unusual double-i; confirm exact legal spelling.
- Real Privacy Policy / Terms pages (links removed until they exist; legal copy shouldn't be invented).
- Firebase: confirm Firestore security rules are published and swap the demo admin credentials before go-live.

## Verified after fixes
Post-JS DOM dump: 20 product cards, 8 cert plates, 5 FAQ items, market legend and
footer group all inject; zero `{CURLY}` placeholders leak; hero renders clean at
1440. No JS parse/boot errors.
