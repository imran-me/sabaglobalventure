# Brand / story photography

One image is referenced here today:

- **`delta-harvest.jpg`** — the framed photo beside the "Our Story" section
  (`sections/about.html`). Suggested subject: harvest being loaded at a Bangladeshi
  river port, a paddy field at golden hour, or your own team/warehouse.
  **Aspect:** roughly 4:3 or 3:2 landscape, ~1400px wide, under ~400 KB.

Until it exists, `img-fallback.js` paints a branded blue/gold placeholder, so the
section still looks deliberate.

You can also change this image without touching any file:
**Admin → Page Content → Story image** (upload, or paste a Google Drive share link).
That override wins over the committed file.
